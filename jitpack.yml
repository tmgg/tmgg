# gitpack 发布，发布速度比中央仓库要快一些

# 1. 提交一个TAG，如1.1.28，注意版本号不能用v打头
# 2. 访问 https://jitpack.io/#io.github.tmgg/tmgg
# 3. 点击指定版本 GET，

before_install:
  - chmod +x mvnw
install:
  -  ./mvnw install -DskipTests -Drevision=$VERSION
