# gitpack 发布，发布速度比中央仓库要快一些
# https://jitpack.io/#io.github.tmgg/tmgg
install:
  - env
  - echo "Running a custom install command"
  - ./mvnw install -DskipTests -Drevision=0.0.2
env:
  VERSION: "0.0.2"
